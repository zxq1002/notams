---
phase: 03-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, tests/__init__.py, tests/test_msa_parser.py]
autonomous: true
requirements: [VALID-4.2]

must_haves:
  truths:
    - "Automated tests cover all 6 patterns of parse_msa_time in MSA_NAV_SEARCH.py"
    - "Automated tests cover multiple coordinate formats in parse_coordinates"
    - "Test suite runs and passes via pytest command"
  artifacts:
    - path: "tests/test_msa_parser.py"
      provides: "Unit tests for MSA parsing logic"
      min_lines: 50
    - path: "requirements.txt"
      provides: "Dev dependencies (pytest)"
  key_links:
    - from: "tests/test_msa_parser.py"
      to: "service/fetch/MSA_NAV_SEARCH.py"
      via: "import and test"
---

<objective>
Establish an automated testing foundation using pytest to ensure the stability of NOTAM parsing logic, specifically focusing on the MSA_NAV_SEARCH module which contains complex regex-based parsing for Chinese dates and coordinates.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@service/fetch/MSA_NAV_SEARCH.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Testing Environment</name>
  <files>requirements.txt, tests/__init__.py</files>
  <action>
    - Add `pytest>=8.0.0` to `requirements.txt` under a new `# Dev Dependencies` section if it doesn't exist.
    - Create the `tests/` directory if it doesn't exist.
    - Create an empty `tests/__init__.py` to make it a package.
  </action>
  <verify>
    <automated>grep -q "pytest" requirements.txt && test -d tests && test -f tests/__init__.py</automated>
  </verify>
  <done>Testing environment initialized with pytest installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Unit Tests for MSA Parser</name>
  <files>tests/test_msa_parser.py</files>
  <behavior>
    - parse_coordinates: Should handle formats like 'DD-MM.mmN DDD-MM.mmE', 'DD-MM.mmN/DDD-MM.mmE', and 'DD-MM.mmNDDD-MM.mmE'.
    - parse_msa_time: Should correctly parse all 6 patterns identified in the source (e.g., 'YYYY年MM月DD日HHMM时至HHMM时', 'MM月DD日HHMM时至DD日HHMM时').
    - preprocess_text: Should strip { } and % % tags and normalize whitespace.
    - infer_year: Should handle year wrap-around for month comparisons.
  </behavior>
  <action>
    - Create `tests/test_msa_parser.py`.
    - Import functions from `service.fetch.MSA_NAV_SEARCH`.
    - Write unit tests using `pytest` for all mentioned functions.
    - Use sample data from `README.md` or actual NOTAM examples for test cases.
    - Ensure tests pass locally.
  </action>
  <verify>
    <automated>pytest tests/test_msa_parser.py</automated>
  </verify>
  <done>Comprehensive unit tests implemented and passing for MSA parser logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External Data → Parser | Untrusted NOTAM strings from海事局 website pass through parser |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | MSA Parser | mitigate | Automated tests cover malformed input and edge cases to ensure parser doesn't crash or return invalid data. |
| T-03-01-02 | Denial of Service | MSA Parser | mitigate | Ensure regex patterns in parser are efficient and tests cover large/repetitive inputs to prevent ReDoS. |
</threat_model>

<verification>
Run `pytest` to confirm all tests pass.
</verification>

<success_criteria>
- pytest is added to requirements.
- tests/test_msa_parser.py exists and contains >50 lines of test code.
- `pytest tests/test_msa_parser.py` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation/03-01-SUMMARY.md`
</output>
