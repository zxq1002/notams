---
phase: 03-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: false
requirements: [VALID-4.1]

must_haves:
  truths:
    - "README.md contains Python 3.9+ requirement"
    - "README.md provides instructions for macOS/Linux setup"
    - "Manual verification confirms app launches and functions on macOS"
  artifacts:
    - path: "README.md"
      provides: "Cross-platform installation and usage guide"
---

<objective>
Update project documentation to reflect the new cross-platform support and Python 3.9+ requirements, and perform a final manual smoke test on macOS to ensure full functionality.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md for Cross-platform Support</name>
  <files>README.md</files>
  <action>
    - Update the "二、关于这个小工具" or "三、工具使用方法与功能介绍" section.
    - Explicitly state Python 3.9+ requirement.
    - Mention that `pywin32` is no longer required.
    - Add instructions for macOS/Linux users:
      - `pip3 install -r requirements.txt`
      - For macOS, note that `pywebview` uses WebKit.
      - For Linux, mention possible dependencies like `python3-tk` or GTK/Webkit libraries if `pywebview` fails to initialize.
    - Update the "运行目录中的notamChecker.exe" part to prioritize `python3 main.py` for cross-platform users.
  </action>
  <verify>
    <automated>grep -q "Python 3.9+" README.md && grep -q "macOS" README.md</automated>
  </verify>
  <done>README.md updated with cross-platform instructions and version requirements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final Manual Verification on macOS</name>
  <what-built>The cross-platform version of notams (Phase 1, 2, 3 components).</what-built>
  <how-to-verify>
    1. Open a terminal on a macOS machine.
    2. Clone/pull the latest changes.
    3. Install dependencies: `pip3 install -r requirements.txt`.
    4. Run the application: `python3 main.py`.
    5. Verify:
       - The GUI window opens without errors.
       - The map loads correctly.
       - NOTAMs are fetched and displayed (check logs/UI).
       - Dialogs (if any triggered) use native macOS style via pywebview.
       - "Copy" functions (if used) work via pyperclip.
  </how-to-verify>
  <resume-signal>Type "approved" if smoke test passes on macOS.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Documentation → User | User follows setup instructions; documentation must be accurate to prevent misconfiguration |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | README.md | mitigate | Ensure no sensitive information (API keys, internal paths) is accidentally added to README. |
</threat_model>

<verification>
Check README.md content and wait for human verification checkpoint.
</verification>

<success_criteria>
- README.md contains "Python 3.9+" and setup instructions for macOS/Linux.
- Human verification for macOS smoke test is completed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation/03-02-SUMMARY.md`
</output>
