---
phase: 01-core-compatibility
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified: ["service/server.py", "main.py", "config.py", "service/fetch/dinsQueryWeb.py", "service/fetch/FNS_NOTAM_SEARCH.py", "service/fetch/MSI_NAV_SEARCH.py", "service/fetch/MSA_NAV_SEARCH.py"]
autonomous: true
requirements: ["ENHANCE-3.1"]

must_haves:
  truths:
    - "Static assets (JS, CSS, images) load correctly on macOS"
    - "Templates are correctly resolved using absolute paths"
    - "Fetching still works and debug/cache files are created"
  artifacts:
    - path: "service/server.py"
      provides: "Refactored server with pathlib"
    - path: "config.py"
      provides: "Refactored config loader with pathlib"
---

<objective>
Refactor path handling in core files and data fetchers to use `pathlib` for better cross-platform support and ensure static assets/templates are correctly located.

Purpose: Fix hardcoded paths and relative path issues that cause failures on macOS.
Output: Cross-platform path handling using modern Python standards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-compatibility/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Core Path Handling</name>
  <files>service/server.py, main.py, config.py</files>
  <action>
    Refactor `service/server.py`, `main.py`, and `config.py` to use `pathlib.Path` for all path operations.
    - Define a `BASE_DIR` in `config.py` using `Path(__file__).resolve().parent`.
    - In `service/server.py`, set `template_folder` and `static_folder` using absolute paths derived from `BASE_DIR`.
    - Update the `/scripts/` and `/statics/` routes to use `Path` methods for joining paths.
    - Remove all usages of `os.path.join`, `os.path.abspath`, and manual string concatenations for paths.
  </action>
  <verify>
    Run `python main.py` and verify that the application starts and can serve the index page and static assets without path errors.
  </verify>
  <done>Core files refactored to use pathlib; assets load successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Fetcher Path Handling</name>
  <files>service/fetch/dinsQueryWeb.py, service/fetch/FNS_NOTAM_SEARCH.py, service/fetch/MSI_NAV_SEARCH.py, service/fetch/MSA_NAV_SEARCH.py</files>
  <action>
    Refactor all data fetcher modules in `service/fetch/` to use `pathlib.Path`.
    - Replace `os.path` and `open()` with `Path` methods.
    - Ensure debug HTML and JSON cache files are written to absolute paths relative to the module or project root.
    - Use `Path.mkdir(parents=True, exist_ok=True)` if any new directories are created.
  </action>
  <verify>
    Trigger a fetch operation (e.g. from the UI or via curl) and check that the expected cache files (e.g. `dinsQueryWeb_response.html`) are created in the correct directories.
  </verify>
  <done>Fetcher modules refactored to use pathlib; cache files handled correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- File system access for logging and caching.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Cache Files | accept | Cache files are for debug/cache only; no sensitive data. |
</threat_model>

<success_criteria>
- No hardcoded backslashes in any refactored files.
- Application templates and static files resolve correctly on macOS.
- Fetching operations work without file path errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-compatibility/01-02-SUMMARY.md`
</output>
