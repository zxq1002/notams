---
phase: 02-enhancement
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified: ["service/server.py", "main.py", "web/scripts/notification.js", "web/static/styles.css", "web/scripts/dataFetching.js", "web/scripts/exportImage.js", "web/scripts/notamsDrawer.js"]
autonomous: true
requirements: ["COMPAT-1.2", "ENHANCE-3.1"]

must_haves:
  truths:
    - "File dialogs use pywebview native API instead of tkinter"
    - "UI uses styled notifications instead of browser alerts"
    - "Application handles BROWSER_MODE gracefully without pywebview dialogs"
  artifacts:
    - path: "service/server.py"
      provides: "Native dialog integration"
    - path: "web/scripts/notification.js"
      provides: "Enhanced notification logic"
    - path: "web/static/styles.css"
      provides: "Notification styling for success/error/info"

  key_links:
    - from: "main.py"
      to: "service/server.py"
      via: "set_window() call"
      pattern: "set_window\\(window\\)"
---

<objective>
Replace `tkinter` file dialogs with `pywebview` native APIs and upgrade the notification system to eliminate browser `alert()` calls, improving cross-platform consistency and user experience.

Purpose: Remove the `tkinter` dependency and provide professional-looking UI feedback.
Output: Integrated native file dialogs and a multi-type notification system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-compatibility/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge webview window to Flask and replace tkinter dialogs</name>
  <files>service/server.py, main.py</files>
  <action>
    Integrate `pywebview` native file dialogs by bridging the window instance to the Flask server.
    - In `service/server.py`, add a global `_window` variable and a `set_window(window)` function.
    - In `main.py`, import `set_window` and call it after `webview.create_window`.
    - In `service/server.py`, refactor `save_image` to use `_window.create_file_dialog(webview.SAVE_DIALOG, ...)` instead of `tkinter.filedialog.asksaveasfilename`.
    - Handle the `BROWSER_MODE` case (where `_window` is None) by logging a message or using a simple fallback if possible.
    - Remove the `from tkinter import filedialog` line from `save_image`.
  </action>
  <verify>
    Run the application in webview mode, trigger "Save Image", and verify a native file dialog appears. Run in BROWSER_MODE and verify no crash occurs.
  </verify>
  <done>Native file dialogs implemented via pywebview; tkinter removed from server.py.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade UI Notification System</name>
  <files>web/scripts/notification.js, web/static/styles.css</files>
  <action>
    Enhance the notification system to support different message types and styles.
    - Update `web/static/styles.css` to add classes `.notification-success`, `.notification-error`, and `.notification-info` with appropriate background colors (e.g., green, red, blue).
    - Update `web/scripts/notification.js`'s `showNotification` function to apply these classes based on the `type` parameter.
    - Ensure notifications are readable and well-positioned.
  </action>
  <verify>
    Call `showNotification('test', 'error')` from the browser console and verify a red notification appears.
  </verify>
  <done>Notification system supports multiple types with distinct styling.</done>
</task>

<task type="auto">
  <name>Task 3: Eliminate Browser Alerts</name>
  <files>web/scripts/dataFetching.js, web/scripts/exportImage.js, web/scripts/notamsDrawer.js</files>
  <action>
    Replace all `alert()` calls with `showNotification(message, 'error')` or 'success' as appropriate.
    - Scan the specified files and replace `alert(...)` with `showNotification(..., 'error')`.
    - For successful actions (if any used alerts), use `showNotification(..., 'success')`.
  </action>
  <verify>
    Trigger an error (e.g., fetch failure or invalid input) and verify a notification appears instead of an alert box.
  </verify>
  <done>All alert() calls replaced with showNotification().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Communication between Python and JS via Flask routes.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | Notifications | accept | Notifications only show generic error messages or filenames. |
</threat_model>

<success_criteria>
- Native file dialogs functional on all target platforms without tkinter.
- User interface feedback is non-blocking (using notifications instead of alerts).
</success_criteria>

<output>
After completion, create `.planning/phases/02-enhancement/02-01-SUMMARY.md`
</output>
